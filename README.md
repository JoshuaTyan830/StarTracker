# StarTracker：歷年繁星推薦數據分析與落點分析平台

## Proposal Report

### 動機與目標
繁星推薦志願選填時，歷年錄取標準（校排%、學測門檻）散落在各年度獨立網頁中，導致跨年份、跨校系的趨勢比對極度耗時且難以精確量化。本專案的目標是整合近 10 年結構化數據，打造一個具備智慧換算功能的分析平台。透過「累積人數百分比」與「時間權重演算法」，將歷史數據轉化為具參考價值的落點預測，協助學弟妹從海量資料中快速篩選最佳志願。

### 競品比較
目前市面上的繁星落點分析工具（如：交叉查榜）雖然具備基礎的條件篩選功能，但仍有以下顯著缺點：
1. **落點預測過於籠統**：分析結果缺乏精確的機率量化，僅粗略分為「有機會通過」與「不確定會通過（夢幻科系，別忘了選）」兩種極端分類，無法提供學生更細緻的風險評估與志願排序依據。
2. **歷年標準排版糟糕且缺乏視覺化**：點擊查看「最近三年錄取標準」等歷史數據時，介面僅以生硬且排版擁擠的純文字條列，閱讀體驗極差；同時缺乏直觀的圖表呈現，使用者難以察覺分數或錄取率的長期波動趨勢。
3. **整體使用者體驗（UX）不佳**：介面設計停留在早期網頁風格，版面充斥大量廣告區塊與繁瑣的表單勾選，不僅干擾視覺，在跨校系對比時也需要頻繁切換頁面，操作不夠直覺。
4. **個資隱私疑慮**：傳統平台多採用伺服器端運算，使用者的學測成績、在校成績等隱私數據必須回傳至遠端主機。

**StarTracker 的核心優勢**：主打「精準量化的加權預測」、「現代化響應式介面」與「動態圖表視覺化」，並採用「純前端本地運算」架構，確保零延遲體驗的同時完全保障使用者成績隱私。

### 預期功能
1. **歷年趨勢視覺化**：動態呈現單一校系近 10 年錄取百分比及學測級分變動折線圖。
2. **跨校系平行對比**：支援選取多個校系進行橫向對比，一目了然各校系競爭強度的消長。
3. **個人化智慧落點分析**：
    * **等值級分換算**：根據當年度與歷年的人數百分比區間，自動計算不同年份間的「等值級分」。
    * **加權預測模型**：引入時間權重（如：愈近年度權重愈高），計算各校系之推薦指數。
4. **動態志願序試排**：提供拖拉式清單介面，讓使用者自訂志願順序並即時進行風險評估。
5. **純本地端安全運算**：所有成績輸入與分析皆於瀏覽器端完成，確保使用者個資（成績）絕不外傳至伺服器。

### 使用技術
* **資料工程**：使用 Python 進行歷年原始簡章與分發結果的清洗（Cleaning）與 JSON 結構化。
* **網頁前端**：React.js 搭配 Tailwind CSS 構建響應式與現代化介面。
* **數據視覺化**：Chart.js 或 Recharts 實作動態趨勢圖表。
* **部署平台**：GitHub Pages (採靜態網頁架構以極小化資安風險)。

### Prototype 預計可驗證內容 (Week 11 檢核點)
為確保專案核心邏輯與技術架構具備可行性，Prototype 階段預計完成並驗證以下項目：
1. **結構化資料庫建置**：展示由 Python 自動化爬蟲與資料清洗管線所產出的高質量 JSON 核心資料檔（涵蓋至少單一學年度的完整校系檢定標準與名額）。
2. **極速檢索與前端串接**：完成基礎 React 網頁雛形，驗證能將龐大 JSON 資料載入前端，並實作透過校系名稱或代碼即時檢索、渲染結果的功能。
3. **單一年度落點條件篩選器**：實作基礎比對演算法，使用者輸入特定學測級分與在校成績後，系統能準確篩選出符合「第一階段檢定標準」的校系清單，驗證核心邏輯的正確性。

---

## Prototype Report

### 目前進度
*   **資料庫建置：** 已完成 112 至 115 學年度繁星推薦錄取標準資料的清洗與格式化（JSON 格式）。
*   **前端介面雛形：** 成功建置基於 React + Vite + Tailwind CSS v4 的現代化前端展示面板（Prototype）。
*   **核心功能實作：**
    *   實作全端搜尋列（支援校系代碼、校名、系名關鍵字搜尋）與學群下拉篩選器。
    *   建立校系代碼對照表（Mapping），自動將代碼轉換為完整的學校名稱。
    *   完成「歷年錄取標準比序」Modal 彈出視窗，將官方 PDF 中複雜的檢定標準（轉為視覺化標籤）與分發比序結果（轉為清晰表格）進行完美重構與並排比較。
    *   於 UI 介面中成功預留並設計「智慧級分轉換」的視覺化預覽元件（Mockup）。

### 遇到的困難
*   **資料去重邏輯「誤殺」外加名額：**
    *   *問題：* 在清理 JSON 資料時，發現「一般生」與「外加名額」會共用同一個校系代碼（如森林系與森林系【外加】皆為 `00151`），導致去重程式將其視為重複資料而覆蓋。
    *   *解決方式：* 引入資料庫工程中的「複合鍵（Composite Key）」概念，將「代碼 + 系所名稱」綁定作為唯一識別碼，成功保留所有獨立名額。
*   **前端樣式框架（Tailwind CSS）版本衝突：**
    *   *問題：* 專案初始化時遭遇 Tailwind CSS 最新 v4 版本的底層架構大改版，導致舊有的 PostCSS 設定檔寫法失效且不斷報錯。
    *   *解決方式：* 查閱最新官方文件，全面升級為 Vite 專屬的 `@tailwindcss/vite` 插件架構，並執行物理刪除殘留設定檔與清除 Vite 暫存（`--force`），成功排除環境報錯。
*   **版本控制（Git）推送衝突：**
    *   *問題：* 嘗試將完成的 Prototype 推送至 GitHub 時，因遠端倉庫存在本地端缺乏的更新檔案，導致 `git push` 被拒絕（Rejected）。
    *   *解決方式：* 執行 `git pull origin main` 將雲端進度拉取下來並完成合併（Merge）後，再次推送即順利完成備份。

### 下一步計畫
*   **開發級分轉換演算法：** 載入 `gsat_historical_stats.json`（歷年學測累積人數統計資料），撰寫「累積人數換算函數」，以推算不同年份間等值的級分落點。
*   **串接動態數據：** 將算好的級分轉換函數與前端 Modal 介面正式串接，取代現有的靜態預覽徽章，實現真實的跨年份級分轉換對照功能。
*   **細節優化：** 持續微調前端 UI 排版細節與使用者體驗（UX），確保系統在展示時的流暢度與穩定度，為期末的 Final Report 做好準備。

---

## Final Report

### 專案說明

StarTracker 是一套**純前端、靜態部署**的繁星推薦資料分析平台，整合 106–115 共 10 個學年度的校系檢定標準、分發比序與招生名額，協助使用者跨年份、跨校系查詢與比對，並在瀏覽器本地完成成績隱私敏感的檢定判斷。

相較 Proposal 的原定藍圖，期末版本已完成以下核心能力：

| 功能 | 說明 |
|------|------|
| **校系資料庫** | Python 管線清洗 PDF 簡章 → JSON；複合鍵區分一般生／外加名額；跨年代碼變更以 anchor 整合 |
| **主頁瀏覽** | 關鍵字搜尋、學群／學校／招生年份／名額類型篩選；對照學年切換；篩選狀態 session 持久化 |
| **第一階段檢定** | 輸入學測六科＋英聽，篩選通過對照學年簡章檢定之校系 |
| **歷年錄取標準** | Modal 橫向對照各學年；在校學業趨勢折線圖；等值級分（累積人數百分比換算）；歷年通過／不通過標示 |
| **校系比對** | 最多 4 校系並排比較檢定、比序與歷年趨勢 |
| **線上部署** | GitHub Pages 自動部署，demo 網址可公開分享 |

**尚未實作、列為後續工作：** 加權預測模型、志願序試排、按需載入資料以縮短首次開啟時間。

**系統架構（簡述）：**

```
PDF 簡章 ──► Python 清洗管線 ──► cleaned_data/*.json
                                      │
                                      ▼ copy_to_public
                              frontend/public/data/
                                      │
                                      ▼ 靜態 fetch
                         React 前端（索引、篩選、換算、圖表）
                                      │
                                      ▼
                              GitHub Pages 部署
```

所有成績輸入、檢定判斷、等值換算皆在瀏覽器端完成，不上傳伺服器。

---

### 使用方式

**執行方式：** 以瀏覽器開啟 GitHub Pages 線上版：[https://joshuatyan830.github.io/StarTracker/](https://joshuatyan830.github.io/StarTracker/)（無需安裝，成績僅存於本機瀏覽器）。

#### 1. 進入與等待載入

開啟網址後，頁面上方可選**對照學年**（預設 115）。待主表載入完成（表頭顯示校系筆數）即可操作；背景會繼續載入其他學年，供歷年 modal 使用。

#### 2. 主頁：瀏覽與篩選校系

- **搜尋列**：輸入校系代碼、校名或系名關鍵字。
- **篩選條件**（按「篩選」開啟）：可設定招生年份、名額類型（一般／外加）、學群、學校；已選條件顯示於搜尋列下方，可個別清除。
- **主表**：依學校分組顯示；每列為一個招生管道（一般生與外加名額分開列計）；點擊列可開啟該校系詳情。

#### 3. 主頁：輸入成績與檢定篩選

- 在「我的學測成績 · 第一階段檢定」區塊輸入六科級分與英聽。
- 勾選「僅顯示通過檢定校系」後，主表只保留對照學年簡章下**第一階段學測、英聽**皆達標的校系。
- 成績與篩選條件在**同一分頁重整後仍會保留**（關閉分頁後清除）。

#### 4. 歷年錄取標準（Modal）

點主表任一校系後開啟：

- **左側**：在校學業一輪／二輪篩選百分比趨勢圖。
- **右側**：橫向滑動對照各學年；各欄顯示檢定、分發比序（部分校系含術科考）；非對照學年檢定旁有紫色 **≈對照學年…級分**（等值換算）。
- 若已輸入成績，各學年標題旁會顯示**通過**或**不通過**（僅代表該年第一階段檢定）。
- 「歷年錄取標準」旁的 **?** 可查看換算與判定說明。

#### 5. 校系比對

- **加入校系**（最多 4 個）：
  - 主頁表格列或歷年 modal 中的「加入比對」；
  - 或進入 **校系比對** 頁，點左側空槽的 **＋**，在搜尋視窗以代碼／校名／系名查找並加入。
- 比對頁上方圖表可切換**第一輪／第二輪**在校學業比序；下方歷年表格可切換**檢定標準／分發比序**並排對照。
- 比對頁可並排對照各校系檢定、比序與歷年趨勢；點側邊欄校系名稱可再開歷年 modal。
- 比對清單在重整後仍會保留，直到手動移除或清除。

---

### 效能分析跟比較

本專案有兩處明確的「資料結構／演算法取捨」，以下以**實際量測**（Node.js，`cleaned_data` / `115.json`）說明。

#### 1. 校系索引合併：`Map` vs 線性陣列查找

**功能流程：** 載入各學年 JSON 後，`buildDepartmentIndex()` 將數萬筆校系列合併為跨年度索引。每一列以複合鍵 `school_id + dept_id + is_extra_quota` 識別。

| 作法 | 資料結構 | 單年合併（2,839 列 × 5 次） | 時間複雜度（n 列） |
|------|----------|------------------------------|-------------------|
| **A（採用）** | `Map` 雜湊表 | **約 1.4 ms** | 平均 O(n) |
| **B（對照）** | `Array` + `findIndex` | **約 665 ms** | O(n²) |

**量測結果：** Map 約快 **460 倍**。  
10 個學年全載時，若用陣列線性查找，合併成本會隨資料量平方成長；`Map` 以 O(1) 更新 `byYear`，是主表即時篩選能維持流暢的關鍵。

**結論：** 對「大量列的去重／合併」場景，雜湊表優於每次掃描整個陣列。

---

#### 2. 等值級分換算：線性掃描 vs 二分搜尋

**功能流程：** 歷年 modal 開啟時，將各年檢定門檻換算至對照學年。算法在 `convertLevelBetweenYears()`：先查來源級分的累積人數百分比，再在目標年度找最接近的級分。

學測級分為**離散 0–15 級**（實務上約 10–16 個有效級分），且同百分比差距需**取較高級分**作 tie-break。

| 作法 | 演算法 | 200,000 次換算 | 單次約略 |
|------|--------|----------------|----------|
| **A（採用）** | 線性掃描全部級分 | 177.5 ms | 0.89 µs |
| **B（對照）** | 二分搜尋 + 鄰域修正 | 163.9 ms | 0.82 µs |

**量測結果：** 二分搜尋僅快約 **8%**，且因累積百分比並非嚴格單調、需同差距取較高級分，簡化版二分在 6,080 組測試中與線性結果**不一致**。

**結論：** 搜尋空間上限約 16，線性掃描程式簡單、語意正確、效能已遠小於 DOM 渲染成本；這是「大 O 較優 ≠ 實務較優」的案例。**演算法選擇應配合資料規模與正確性約束，而非一律追求 O(log n)。**

---

#### 3. 已知效能瓶頸（非演算法問題）

| 項目 | 現況 | 影響 |
|------|------|------|
| 首次載入 | 對照學年 JSON ~7 MB；10 年全載 ~69 MB | 網路與 JSON parse 為主要延遲 |
| 背景載入 | 進站後依序 fetch 全部學年 | 歷年 modal 後續開啟較快 |
| 渲染 | 主表虛擬化未實作 | 篩選後數千列仍可行，但非瓶頸主因 |

**後續優化方向：** 按需載入學年、輕量 browse index、部署 gzip/brotli 壓縮。

---

### 期末成果與後續計畫

**相較 Prototype 的進展：**

- 資料由 4 年擴至 **10 年**；等值級分由 Mockup 改為**真實換算**並串接歷年通過判定。
- 新增校系比對頁、檢定篩選、GitHub Pages 部署與篩選持久化等 UX 細節。
- 針對使用者易混淆處（換算方向、通過／不通過語意）補充介面說明。

**後續計畫（暑假）：**

1. 時間加權落點預測模型  
2. 志願序試排介面  
3. 按需載入與索引分片，改善首次開啟速度  

**Demo 影片：** （待錄製，建議展示：主頁篩選 → 成績檢定 → 歷年 modal 等值級分 → 校系比對）

